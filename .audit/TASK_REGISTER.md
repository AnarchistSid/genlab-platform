# GenLab task register — filed 2026-09-11 (OPS-06 D)
Filed, not started. M=measured, I=inferred, D=documented.

| id | title | one line | class |
|---|---|---|---|
| **T-06** | Characterise the prod test baseline | **201 failing node IDs, identical in both trees** (measured 09-11 via `baseline_compare`); 189 failed + 12 errors each side. Classify each by cause (env/credential, fixture rot, real defect, flaky). No repairs. | M |
| **T-07** | Cascade docs stale; voice gate must key on config | Live cascade head is `infsh_inworld`, not the documented ElevenLabs→OpenAI→Edge→gTTS. Correct the docs; specify the hero-format voice gate as `audio_provider ∈ config-declared hero tiers AND degraded == false`, never a hardcoded provider name. Blueprint now carries both fields. | M |
| **T-08** | Proportional retry haircut cannot recover an overshoot | The 85% retry cuts budget and script proportionally, preserving the overshoot ratio (story B: +0.29s → +0.42s *after* retry). Every `script_too_long` degrades by construction. Derive the retry cap from measured overshoot; gate on a previously-degrading blueprint passing on retry. | M |
| **T-09** | Clean-baseline testing needs mirrored machine state | A git worktree reconstructs the repo, not the machine. Collection depends on untracked models/runtime/caches **and two gitignored operator configs**. Superseded in practice by `scripts/baseline_compare.sh`; keep as the rationale record. | M |
| **T-10** | `pytest-baseline-diff` app parser defect | Its `_OUTCOME` regex matches ERROR-level log lines at column 0; needs the `::` node-id requirement. Shipped app, requires redeploy. | M |
| **T-12** | Suite provisioning spec | Collection depends on gitignored state. Spec = B.12's 19 prod-path files + T-11's two gitignored configs (`affiliate_catalog.yaml`, BB `persona.yaml`) + T-14c's divergent render path. Explains local ~516 vs prod 11,225 collection. | M |
| **T-13** | Waiter helper, PID-only | Two self-matching incidents: `pgrep -f` matched the waiter's own command line (3 loops spun 30 min, one silently blocked a launch); `pkill -f` then killed the ssh session. Encoded in `baseline_compare.sh`; extract as a shared helper. | M |
| **T-14a** | `caption_animator` filtergraph defect | `No such filter: '0.000'` (gaming), `'23.700'`/`'21.000'` (ai_creators) — a **time value lands where a filter name belongs**. ffmpeg exit 8. Not style-specific: movies ran identical style+count and succeeded. Discriminator is segment timing. | M |
| **T-14b** | Exception swallowed to WARNING, stage reports success | ffmpeg exit 8 logged at WARNING; **zero ERROR/CRITICAL lines** in the run; `RenderGamingVideo` and `ValidateVideos` both reported completion; a `< SPEC.min_duration → return base composite` guard then shipped the untransformed clip. **A class, not an instance** — enumerate every swallowed-to-WARNING fall-through and every return-base-composite guard. Rule #19. | M |
| **T-14c** | Gaming render output path diverges | Gaming writes `CriticalRush/.tmp/rendered/<run_id>/`; the other four use `/opt/genlab/.tmp/runs/`. Single-path audits report a false zero — INV-01b did. Converge, or register both paths in audit tooling. | M |
| **T-15** | Approval path — corrected wording | **The publisher requires approval; scheduling is not approval.** Gate: `action_taken == "approved"` (`platforms/gatekeeper.py:88`, hardened by R-08). **No direct writes to any of** `action_taken`, `reviewed_at`, `scheduled_for`, `action_taken_source`, `auto_approval_confidence` **outside the auto-approver's `update_fields`** — the sanctioned path stamps all five together by construction. | M |
| **T-16** | Zero-cut reels in three niches | anime, movies, sports ship single-clip 16–19s renders with **0 scene cuts**; only ai_creators cuts (9, 0.294/s). The one-borrowed-clip ceiling measured; the playbook's pattern-interrupt requirement fails by construction on the footage path. | M |
| **T-18** | Harness adoption | Every future fix prompt's "existing tests pass" gate is replaced by **"set (b) empty via `scripts/baseline_compare.sh`"**. The 201 baseline is the reference until T-06 shrinks it. | — |
| **T-19** | INV-01d — deferred render rows | Ducking, transitions, attribution slate for the four rendered niches from `.audit-retention/2026-09-11/`. Not time-pressured; opens after the 09-12 observation, each probe validated against a control first. | — |
| **T-20** | Probe-validation rule (standing methodology) | Any new measurement probe reports against a **positive control** before its first real reading. Case: three false zeros in one day — the retention `find` matching no MP4s, the scene-cut probe returning 0 where `showinfo` finds 9, and `find -maxdepth 1` reading a symlinked `.tmp` as empty. Not a code task. | M |

**T-17 — CLOSED, not filed.** Stale-slot rows are handled: `_SCHEDULE_STALE_AFTER = 18h` blocks them (`gatekeeper.py:19`) and two archiver timers sweep them (`archive-stale-visuals` 21:30Z, `archive-stale-drafted` 04:15Z). (M)

**T-11 — CLOSED 2026-09-11.** The 13 skip-delta tests gate on two gitignored operator configs; both now mirrored by the harness. (M)

**T-14 — SUPERSEDED** by T-14a/b/c. The "gaming rendered zero MP4s" reading was an artifact of scanning only `/opt/genlab/.tmp/runs/`.

---

## Added 2026-09-11 (OPS-16b)

| id | title | one line | class |
|---|---|---|---|
| **T-26** | Remove the retry-only unit | Gate: 7 consecutive days of full-run journal lines showing `_run_retry_pass` re-attempting prior failures under the 06:35/12:05/18:35 fires. Code read already confirms it is called on the full path (`publish_all_platforms.py:380,710`); this is the observed half. Observation 1 due from the 18:35Z fire on the 12:15Z movies/threads failure. | M |
| **T-29** | Approved-queue ordering is FIFO by approval time | Degraded, script-less blueprints publish ahead of narrated ones: `ca19f6a7` (degraded, 0 chars) holds 09-12 while `3c904e01` (356-char script, not degraded) waits for 09-13. Proposed: when >1 approved candidate waits, order by `degraded = false`, then confidence, then approval time. Config-level. | M |
| **T-27** | TN = 0 in the tuner's confusion matrix | The operator has never confirmed a rejection, so agreement measures half the decision (rule #22's shape). A rejection path — even sampled — is needed before the tuner's calibration means anything. | M |
| **T-28** | Tuner alerts unread | `[ALERT] … the threshold is acting as an off switch, not a filter` was correct for three weeks and reached only the journal. Same class as T-24. | M |

### #227 state as of 2026-09-11

Parity revert **done**: all five `publishing.yaml` at committed values
(0.85 / 0.732 / 0.85 / 0.85 / 0.85), tree clean, both tuner timers
**disabled** (`genlab-gate-tuner` 01:00Z, `genlab-calibration-tuner` 06:15Z;
restoration record in `.audit-retention/2026-09-11/tuner/`).

Mechanism, from the tuner's own log: **ratchet-only-up plus a self-locking
safety rail.** The `.bak` chain shows `0.9 → 0.95 → 0.99 → 1.0` over four days.
Escaping 1.0 needs a delta of −0.075 to −0.090, and `|delta| > 0.05` triggers
`[SKIP APPLY] operator review required` — it can climb in auto-appliable steps
and can only descend in blocked ones. The tuner had already diagnosed it
correctly: *"the threshold is acting as an off switch, not a filter."*

**Still owed:** §C's recomputed bimodal table (low-cluster max, main-cluster
min, proposed midpoint per niche) for the operator's value decision, and
A.3/A.5 (does the tuner ever commit; what "gate approved" counts in its
confusion matrix — 21-in-48h vs 1-in-14d cannot both be `action_taken`).

| **T-30** | Tuner "achievable ceiling" uses the wrong denominator | It is p90 of the `gate_approved` population (rows in `auto_approval_calibration`), which skews high by construction, not of the rendered set. Its suggestions admit 0/24 (movies, 0.925) and 0/11 (sports, 0.896) of rendered blueprints — it would "correct" an unreachable 1.0 to an unreachable 0.925 and log it as a fix. | M |
| **T-31** | Config reads must assert on the ACTIVE line | Any read or edit of a YAML value must count **non-comment** matches and assert exactly one; verification must read that same active line. Never `count=1` on a regex, never a `grep -o` that can match a comment. Case: the 0.65 edit changed a *comment* in `BlackboxBrief/config/publishing.yaml`, the verify step re-matched the same comment and reported success, and it would have shipped green. The assertion `expected 1 ACTIVE min_confidence line, found N` is what caught it — and repairing it revealed BB's real value was 0.715, invalidating three earlier readings. Same family as T-20: a green check that measured the wrong thing. | M |
| **T-32** | Scheduled-job labels drift from cron | A job's stated time must be DERIVED from its cron at registration, never typed alongside it. `b674da45` carried `32 10` (10:32 IST = 05:02Z) while its own description claimed 04:32Z — a 30-minute mislabel from creation, and every reference to that job for two days quoted the label, not the cron. Same shape as T-31: the label and the thing it described drifted apart and only the label was ever read. Determine the scheduler TZ by a DISCRIMINATING test, not a consistent one — a cron whose label crosses a date boundary (day-of-month N labelled as N-1) can only be satisfied by one interpretation. | M |
| **T-34** | Threads captions exceed the 500-char API limit | movies/threads fails at container creation: "Param text must be at most 500 characters long." CLAUDE.md documents Twitter ≤280 but carries no Threads rule and the caption builder enforces none. Every movies Threads publish fails this way. | M |
| **T-35** | Permanent failures classified TRANSIENT | A 500-char overflow fails identically on every retry, yet retry_pass tags it `(TRANSIENT)` and re-attempts. Retry classification must separate length/validation errors from network transients, or the retry budget is spent on attempts that cannot succeed. | M |
| **T-36** | `systemctl show` reports success for non-existent units | `LoadState=not-found` but `Result=success`. Any gate that checks Result alone reads a green light on a phantom unit. Cost: the STEP 0 gate in two scheduled audit jobs pointed at `genlab-pipeline@<niche>.service`, which does not exist, and would have proceeded on five false greens. Rule: always print LoadState beside Result; treat `LoadState != loaded` as INSTRUMENT ERROR, never as a result. | M |
| **T-37** | ~~Publisher `SuccessExitStatus=0 1 3 4` masks real failures~~ **DOWNGRADED 2026-09-12** | The 18:35Z fire exited **1**; systemd recorded **success**. Rule #26 says exit 0 unless a genuine incident — the remedy here was applied by widening SuccessExitStatus instead, so genuine publisher failures now cannot fire OnFailure and cannot be detected by `Result`. Any completion gate on this unit must read `ExecMainStatus`. | M |
| **T-38** | `belt app run` exits 0 on a FAILED task | Body carried `status_text: failed` while the shell saw exit 0. Any wrapper must gate on `status_text == completed`, never the exit code. Rule #26's shape inverted: a failure presenting as success. | M |
| **T-39** | A cost reading is valid only paired with `status_text == completed` | A probe on a failed call reads $0.0000 and is indistinguishable from a free app. The first `loudness-normalize` probe reported "$0.0000 per call" from a run that never did the work. T-20 at the billing layer. | M |
| **T-40** | Provider limits differ per app; the same batch/prompt cannot be reused | ElevenLabs caps at **2 concurrent requests** (5-wide batch 429'd on 2 of 5); Inworld rate-limits harder (4 of 5 failed, 429+503) and caps `design_prompt` at **250 chars** where ElevenLabs has no limit. Serialize EL work ≤2; maintain separate prompt sets per provider. Governs §3's SFX/music batches. | M |
| **T-41** | `loudness-normalize` description contradicts its schema | Prose says "Video in, video out"; the schema says "Audio or video file to normalise." It accepts MP3. Own app — fix the prose. Reading the schema rather than the description is what made CAP-01 §1 possible. | M |
| **T-42** | Approver window does not cover the pipeline stagger | Pipelines fire ai 02:30 · movies 03:30 · gaming 04:00 · sports 05:00 · anime 06:00Z, but the auto-approver runs `OnCalendar=*-*-* 06..22:00,30:00 UTC`. Nothing rendered between 22:30Z and 06:00Z is approved before the 06:35Z publisher, and anime (rendering AT 06:00Z) is structurally one cycle behind every day — earliest publish 12:05Z. Fix is config-level: extend the approver to 02:00–06:00Z at the same cadence, or add a 06:25Z pass. Not now; file with the stagger table. Relates to T-25, T-29. | H |
| **T-43** | `niche_pauses` has no audit trail; the sweeper destroys the only record | `pause()` is careful to fail CLOSED so a pause is never silently dropped, but once `paused_until` passes, `sweep_expired_pauses` DELETEs the row and who/when/why/expiry are gone. The 09-11 ai_creators pause is unrecoverable for exactly this reason. Fix: archive to a history table on sweep, or soft-delete. | M |
| **T-44** | Journal retention is three different numbers, none matching | `journald.conf` says `MaxRetentionSec=30d` / `SystemMaxUse=2G`; `scripts/disk_cleanup.sh:63` runs `journalctl --vacuum-time=2d` **hourly**; actual oldest entry is **~14h** at 48.6M of the 2G cap. Any incident older than ~14h cannot be investigated from the journal, which is why the pause provenance had to be reconstructed from `gate_examinations` instead. Reconcile the three, and prefer durable DB tables over journald for anything auditable. | M |
| **T-45** | Auto-pause on low reach is a self-sustaining lock-out | `maybe_auto_pause_on_critical` (`compliance/account_health.py:384`, flag `GENLAB_AUTO_PAUSE_ON_HEALTH_CRITICAL=1`) pauses a niche 4h when 48h reach ÷ 14d baseline < 0.1, with UPSERT re-pause on each new signal. A paused niche cannot publish, so it earns no reach, so the next check re-pauses it. ai_creators fired this 8 times in one run and did not publish today despite `ca19f6a7` being approved with an in-window 06:30Z slot. Fix: do not let the pause feed its own input — exempt the window during which the niche was paused from the reach calculation, or require an unpaused publish before re-tripping. | **H** |
| **T-46** | Shadowban detector has no absolute floor | The ratio gate trips on a 14-day baseline of **5** reach: 5 → 0 is "∞x" and critical, though both numbers are noise. `MIN_BASELINE_SAMPLES=5` gates sample COUNT, never MAGNITUDE, so the detector fires hardest on the low-reach channels that most need to keep publishing. Fix: add `GENLAB_HEALTH_MIN_BASELINE_REACH` (suggest ≥50) below which the check returns `healthy` regardless of ratio. | **H** |
| **T-47** | `is_paused()` fail-opens to False when DATABASE_URL is absent | `_connect()` returns None without the env var and `is_paused` returns False — indistinguishable from "not paused". Measured both ways on the same host: env-less → False; `set -a; . .env; set +a` → True plus the full row. This produced a false "all five niches paused=False" reading at 02:35Z and was written into three scheduled audit jobs. Fix: fail CLOSED, or return a tri-state so callers can distinguish unknown from not-paused. Any probe must source `.env` first. | **H** |
| **T-48** | "No cap-available slot" is logged as a per-blueprint ERROR | A saturated 7-day queue is normal, expected back-pressure, but the approver logs `errors=8` and `WARNING … had 8 per-blueprint error(s)`. It correctly holds exit 0 per rule #26, but the error counter makes routine saturation look like malfunction and buries genuine errors. Fix: count slot-unavailable as a distinct `deferred_no_slot` tally, not `errors`. | M |
| **T-49** | `auto_approval_confidence` is JSONB-only, never a promoted column | It lives in `blueprints.extra`, so `SELECT auto_approval_confidence` errors outright. Every query must use `(extra->>'auto_approval_confidence')::numeric`. I wrote the bare-column form into all three scheduled jobs and it errored on first contact. Rule #28's neighbour: not a write silently landing in `extra`, but a read assuming a promotion that never happened. | M |
| **T-52** | `hook_equals_title` — writer emits hook identical to title | `83b99a2c` (sports, conf 0.858) died at `render:compositor_failed:pre_render_quality:hook_equals_title`. The pre-render gate is working as designed; the defect is upstream in WritingStrategy, which produced a hook that is just the title. Scope: WritingStrategy, not the gate. Do not widen the gate. | M |
| **T-53** | Queue policy is per-niche and freshness-aware | Replaces T-29's narrated-first framing. Per-niche lookahead + ordering + stale expiry: ai_creators/sports news (2d, newest-first), gaming/movies (4d, conf then newest), anime (5d). Under it, 8 of 16 queued expire and ai_creators regains headroom from 09-15 so fresh candidates stop being refused. **Scoping: the lookahead is a hardcoded `range(0, 8)` in BOTH `auto_approver.py:179` and `dashboard/server/core/publishing_queue.py:434` — one contract, two implementers, must change together. Ordering and expiry do not exist and are new code. Only the cap is config (`platform_caps.yaml`).** Constraint: `3c904e01` keeps 09-13 06:30Z. | **H** |
| **T-54** | Intake throttle — render count from queue headroom | Renders per fire per niche = remaining queue capacity + 1, floor 1, ceiling 4. **Justify on bandit starvation, not cost:** measured spend is $0.1430/day total across five niches, so the 93% discarded share is only ~$0.13/day against a $5/day ceiling. The real loss is that 86 renders produced 6 publishes in 7 days, so 93% of generation returns no reward signal to the learner. | M |
| **T-29** | ~~Queue ordering: narrated-first~~ | **CLOSED — refuted.** ai_creators' two narrated blueprints already hold 09-13 and 09-14 ahead of five degraded ones, so there was nothing to reorder; and narration is ai_creators-only today (gaming/movies/anime are all `degraded=false` with 0-char scripts), so the rule does not discriminate elsewhere. Applying it would have moved `3c904e01` to 09-14 and slipped #218. Superseded by T-53. | — |
| **T-55** | `baseline_compare.sh` prints **PASS when no test ran** | On macOS `setsid` does not exist, so `run_one` fails and both pytest logs contain only `setsid: command not found`. The harness then greps for `^(FAILED\|ERROR)` (0 hits), counts collected (0), computes empty sets and prints `VERDICT: PASS`. It guards inversion (`ADDED_TESTS -ge 0`), skip-delta and import isolation, but **never asserts that a single test executed**. A zero-collection run is indistinguishable from a clean run. Fix: `die` unless `CB > 0 && CH > 0`, and check `run_one`'s exit status rather than only parsing its log. This is the T-20 shape inside the instrument built to prevent T-20. | **H** |
| **T-56** | `baseline_compare.sh` is not portable off the VPS — four defects | (1) `SRC="${GENLAB_SRC:-/opt/genlab}"` defaults to a prod path, so a bare invocation on the Mac aborts with a confusing message; (2) line 75 `[[ "$got" == "$WT"/* ]]` is a raw prefix match, and on macOS `/tmp` → `/private/tmp` makes the import-isolation assertion a FALSE ABORT for any `--parent` under `/tmp`; (3) `setsid` is Linux-only (see T-55); (4) log paths `/tmp/bc_*.log` are hardcoded and world-writable, so a stale root-owned file silently poisons a run. Fix: realpath both sides of the isolation check, drop `setsid` or guard it, and put logs under the parent dir. | M |
| **T-57** | **STANDING RULE — assert the work happened, not that nothing complained** | Four instruments in this audit reported success for work they did not do: the publisher's `SuccessExitStatus=0 1 3 4` (exit 1 read as success, T-37); `belt app run` exiting 0 on a failed task (T-38); `systemctl show` returning `Result=success` for a unit with `LoadState=not-found` (T-36); and `baseline_compare.sh` printing PASS having executed zero tests (T-55). Every one passes a "nothing complained" check. The rule: a success verdict must rest on **positive evidence that the work occurred** — post IDs, a non-zero executed-test count, `LoadState=loaded`, `status_text == completed` — never on the absence of an error. Corollary, learned the hard way today: the first version of such an assertion is often itself too weak. The T-55 guard counted skips and collection errors as "collected" and passed an interrupted run at 19/19; only counting `passed+failed` caught it. **Test the guard against the failure it is meant to catch.** Sits alongside T-20 (positive control before reporting a zero) and T-31 (assert on the active line). | **H** |
| **T-58** | `ast.parse()` does not enforce `__future__` placement — use `compile()` | Chasing the collection abort, I checked `llm_cost.py` with `ast.parse()` three times and got CLEAN each time, at three separate refs and in a plain worktree. `compile()` reports `SyntaxError line 54: from __future__ imports must occur at the beginning of the file`. `ast.parse` is permissive about future-statement position; only `compile()` applies the rule Python's import machinery applies. Every syntax check must use `compile(src, path, "exec")`. Instance of T-57 inside the verification itself: three false greens from the wrong instrument. Cheap pin worth adding: a test that `compile()`s every file under `genlab-core/src`. | M |
| **T-59** | Suite-wide collection abort went unnoticed for a day | `9bed1094` prepended a second module docstring to `llm_cost.py`, making `from __future__` illegal and producing `Interrupted: 18 errors during collection` — which aborts **all 11,152 collected tests**, not just the 18 modules. Nothing surfaced it: the harness was the only thing that ran the full suite, and it was reporting PASS-on-zero (T-55) at the time. Two independent faults hid each other. Fixed in `2abe7a61` (11,337 collected, 0 errors). | M |
| **T-60** | Harness set-difference assumes order-independent tests — it can emit a FALSE RED | The VPS re-run of `0e1abe6b..2abe7a61` returned `VERDICT: FAIL — 1 regression` with set (b)=1 and set (c)=1. Both flagged tests invert between isolated and in-suite execution: `test_linucb_wins_over_thompson_when_enabled` **passes 6/6 standalone at HEAD** but failed in-suite; `test_ratchet_advancement::test_log_line_shape` **fails 6/6 standalone at HEAD** but passed in-suite. Neither touches the diff (LinUCB arm classification and the ratchet advancer vs a loudness change in `validate_videos.py`), and the Mac run over the identical range had set (b)=0. The set-difference method treats any in-suite failure as attributable to the diff, so one order-dependent test manufactures a regression. Fix: re-run set (b) members in isolation before declaring FAIL, and report order-dependent ones separately as `UNSTABLE` rather than folding them into the verdict. Counterpart to T-55: that was a false green, this is a false red. | **H** |
| **T-61** | **T-21 is NOT fixed — running the suite still writes fixture rows to PROD telemetry** | Five `test_*` rows totalling **$10.0088** were written to `pipeline_run_costs` between 10:09:04 and 10:30:37 UTC on 09-12 — the exact window of my VPS `baseline_compare` runs. One is `run_id='test_run'`, `$10.00`, `claude-sonnet-4-6`: the same fixture that triggered T-21 originally. Real spend that day was **$0.2956**, so the fixtures are **34x** real daily spend. `9bed1094` filtered `test_` rows in the READER, which hides them from budget readers but does not stop the WRITE — and the harness's own scan flags the vector on every run ("2 file(s) import prod storage with no mock in-file"). Consequence: a gate advertised as read-only has a production write side-effect, and any raw query over the table (including the LLM-01 cost analysis) is polluted. Fix must be write-side: an autouse fixture that points the storage backend at a mock for the whole suite, not a reader filter. Cleanup of the 5 rows needs operator go. | **H** |
| **T-62** | Two order-dependent tests (not blockers) | Surfaced by T-60's false red. `tests/test_classify_arm_linucb_wire.py::TestLinUCBPathOverridesThompson::test_linucb_wins_over_thompson_when_enabled` **passes 6/6 standalone at HEAD** but fails in-suite. `tests/scheduling/test_ratchet_advancement.py::TestLogEmission::test_log_line_shape` **fails 6/6 standalone at HEAD** but passes in-suite. Both depend on state another test sets or clears — likely module-level bandit/arm state and a logging handler respectively. Neither blocks anything now that the harness reports them as UNSTABLE rather than folding them into the verdict. Fix by making each set up its own state; until then they will keep flapping and keep appearing in the UNSTABLE list. | L |
| **T-63** | `response_format: json_schema` for the writer (routing-independent) | Kept from the declined LLM-01 analysis: structured output is available on the **direct Anthropic path** too, not only via the belt. `base_writing` currently parses free text against a Pydantic model. Enforcing the schema at the API boundary converts a parse failure into a provider-side retry and removes a class of malformed-output handling. Unrelated to which provider is used; file under writer strictness. | M |
| **T-64** | ~40% of ai_creators renders are truncated MP4s | `a9308ff9…` and `d285bccf…` report `moov atom not found` with no duration — 12 MB written, encode never finalised — while three sibling renders are fine at ~31.4s. Two of five recent ai_creators outputs. `caption_animator` degrades correctly (empty filter chain, skip, log) rather than crashing, so this surfaces only as a missing caption. Blocks any gate that picks "the newest render" without checking playability. | M |
| **T-65** | TTS input text is written, spoken, and never stored | anime/movies/sports renders speak GenLab-authored copy — the CTAs "W or L take", "Follow, we watch so you don't", "We clip every moment" are audible in the transcripts — yet the blueprint has no `caption_segments`, `tts_text` or `audio_script` key, and `narration_script` is empty for all three. The only record of what was said is the audio itself. Consequence: forced alignment is impossible, reward attribution cannot see the spoken copy, and a regression in TTS text would be invisible. Persist the exact string sent to TTS. | **H** |
| **T-66** | FIX-T23 gates 3 and 5 have no written definition | Grepping `.audit/` for FIX-T23 gate criteria returns only gate 2 (227_threshold_table.md:190, INV-01b_summary.md:132). Gates 3 and 5 are referenced in scheduled checks and prompts as things that "close at 12:05Z" but no file states what passing them means. An unwritable gate cannot be passed or failed, only asserted. Write the criteria or retire the references. | M |
